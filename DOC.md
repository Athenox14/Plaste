# Plaste — Project Overview

_Auto-generated by `scripts/generate_docs.sh`. Do not edit by hand._

## Backend modules (`src/`)

- **acl.rs** — Access control: owner-or-admin always wins; otherwise defer to the `permissions`
- **admin.rs** — / Row shape for `SELECT id, token, owner, is_admin, quota_bytes, used_bytes, expires_at FROM tokens`.
- **audit.rs** — / Own table for this module; kept separate from db.rs's SCHEMA array (concurrent edits there).
- **auth.rs** — / Returns true if `expires_at` (an rfc3339 timestamp, if present) is in the past.
- **chunk_upload.rs** — Chunk-aware upload protocol: lets a client compute its own content-defined chunks,
- **comments.rs** — / Schema for the comments feature, kept separate from db.rs's SCHEMA array
- **crypto.rs** — / Fixed width every key id is padded/truncated to on the chunk wire format (see
- **db.rs** — / Shared row-mapping helper for queries that only need a single `id` column.
- **files.rs** — / No module-owned tables currently; kept as a no-op hook (called from main.rs) in case
- **folders.rs** — / Row shape for `SELECT owner_token_id, deleted_at FROM folders WHERE id = $1`.
- **fulltext.rs** — / Full-text index over file names + (best-effort) extracted text content.
- **gc.rs** — / Releases one reference to each chunk in `manifest` (call once per permanently-deleted
- **graphql.rs** — ---------- admin: tokens ----------
- **groups.rs** — (no doc comment)
- **keymgmt.rs** — (no doc comment)
- **lib.rs** — (no doc comment)
- **main.rs** — TEMPORARY: startup breadcrumbs for diagnosing a silent-exit-0 bug that only reproduces
- **mcp.rs** — Minimal MCP (Model Context Protocol) server: hand-rolled JSON-RPC 2.0 over
- **ratelimit.rs** — / Builds a per-IP token-bucket rate limit: `burst` requests allowed immediately,
- **retention.rs** — / Hardcoded fallback when no policy row exists at all (global or per-user).
- **search.rs** — Files: tantivy full-text (name+content) ranked search, plus a LIKE fallback
- **share_page.rs** — Human-readable landing page for a public share link, at `GET /s/{share_token}` (plus
- **sharing.rs** — Sharing: public links to a file or folder (optionally password-protected and/or expiring),
- **state.rs** — (no doc comment)
- **storage.rs** — / Directory the master encryption key is persisted under, independent of storage backend
- **storage_backends.rs** — Admin-configurable storage backend definitions (REST). See also `graphql.rs` for the
- **tags.rs** — / Own schema for tags/favorites, run separately from db.rs's SCHEMA array
- **tiering.rs** — Automatic hot/cold chunk tiering ("Tiering hot/cold automatique").
- **trash.rs** — ponytail: admin scoped to their own token id here too — no "view everyone's
- **tus.rs** — tus 1.0.0 resumable upload protocol (Creation + Core + Termination extensions),

## HTTP endpoints

- `/admin/audit-log`
- `/admin/groups`
- `/admin/groups/{id}`
- `/admin/groups/{id}/members/{token_id}`
- `/admin/rotate-key`
- `/admin/storage-backends`
- `/admin/storage-backends/{id}`
- `/admin/storage-backends/{id}/activate`
- `/admin/storage-backends/{id}/deactivate`
- `/admin/tokens`
- `/admin/tokens/{id}`
- `/admin/tokens/{id}/renew`
- `/admin/tokens/{id}/rotate`
- `/audit-log/mine`
- `/chunks/check`
- `/chunks/upload/{hash}`
- `/comments`
- `/comments/{id}`
- `/favorites/{id}`
- `/files/finalize`
- `/files/upload`
- `/files/{id}`
- `/files/{id}/download`
- `/files/{id}/preview`
- `/files/{id}/restore`
- `/files/{id}/signature`
- `/files/{id}/upload-delta`
- `/files/{id}/versions`
- `/folders`
- `/graphql`
- `/mcp`
- `/permissions/{id}`
- `/public/shares/{share_token}`
- `/resource-tags/{id}`
- `/search`
- `/shares`
- `/shares/{id}`
- `/tags`
- `/tags/{id}`
- `/trash`
- `/trash/{id}`
- `/tus/uploads`
- `/whoami`
- `/x`

## File sharing API

Owner routes (require `Authorization: Bearer <token>`; caller must own the resource, or be admin):

| Method | Path | Body / query | Response |
| --- | --- | --- | --- |
| POST | `/shares` | `{resource_type: "file"\|"folder", resource_id: int, permission: "read"\|"write"\|"comment", password?: string, expires_at?: rfc3339}` | `{id, share_token, permission, expires_at}` |
| GET | `/shares` | — | `[{id, share_token, resource_type, resource_id, permission, expires_at, created_at, password_protected, view_count, download_count, last_access_at}]` |
| DELETE | `/shares/{id}` | — | `204`, link stops working immediately |

The public link is `/public/shares/{share_token}`. `share_token` is 128 bits of CSPRNG
randomness, base64url, 22 chars. `expires_at` must parse as rfc3339 or the request is `400`.

Public routes (NO authentication — anyone holding the link):

| Method | Path | Query | Response |
| --- | --- | --- | --- |
| GET | `/public/shares/{share_token}` | `password?` | file: `{kind: "file", name, size, content_type}` · folder: `{kind: "folder", name, children: [name]}` |
| GET | `/public/shares/{share_token}/download` | `password?` | streamed file bytes (files only) |

Public status codes: `404` unknown or revoked token · `410` expired · `401` password missing
or wrong · `429` too many password attempts (5, then 1 per 12s, per share token) ·
`400` download requested on a folder share.

Notes for clients:
- Expiry, revocation and password are re-checked on **every** request; a successful password
  check issues no cookie or session, so revoking a share takes effect at once.
- Downloads always come back as `Content-Type: application/octet-stream` with
  `Content-Disposition: attachment` and `X-Content-Type-Options: nosniff`, regardless of the
  file's real type — user-uploaded HTML must not execute on this origin. Use `content_type`
  from the metadata route for display purposes only.
- Statistics are aggregate only (counts plus last access time). No per-visitor data — no IP,
  user agent or visit history — is recorded.

## Desktop client

- **client/** — plaste-sync
