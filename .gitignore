/target

# runtime data (chunks, hiqlite db, master keys, fts index) — never commit
/data/
*_smoketest_data/

# frontends / tauri
node_modules/
dist/
client/src-tauri/target/
client/src-tauri/gen/

# misc
*.log
.env
nul

# Fuzzing : artefacts locaux. Le corpus est conserve par le cache Actions, pas
# dans le depot — il grossit vite et n'a aucun interet en revue.
fuzz/target/
fuzz/corpus/
fuzz/artifacts/
