# Plaste — Project Overview

_Auto-generated by `scripts/generate_docs.sh`. Do not edit by hand._

## Backend modules (`src/`)

- **acl.rs** — Access control: owner-or-admin always wins; otherwise defer to the `permissions`
- **admin.rs** — / Row shape for `SELECT id, token, owner, is_admin, quota_bytes, used_bytes, expires_at FROM tokens`.
- **audit.rs** — / Own table for this module; kept separate from db.rs's SCHEMA array (concurrent edits there).
- **auth.rs** — / Returns true if `expires_at` (an rfc3339 timestamp, if present) is in the past.
- **comments.rs** — / Schema for the comments feature, kept separate from db.rs's SCHEMA array
- **crypto.rs** — / Fixed width every key id is padded/truncated to on the chunk wire format (see
- **db.rs** — / Shared row-mapping helper for queries that only need a single `id` column.
- **files.rs** — / No module-owned tables currently; kept as a no-op hook (called from main.rs) in case
- **folders.rs** — / Row shape for `SELECT owner_token_id, deleted_at FROM folders WHERE id = $1`.
- **fulltext.rs** — / Full-text index over file names + (best-effort) extracted text content.
- **gc.rs** — / Releases one reference to each chunk in `manifest` (call once per permanently-deleted
- **graphql.rs** — Ensure caller owns (or is admin for) the parent file before listing versions.
- **groups.rs** — (no doc comment)
- **keymgmt.rs** — (no doc comment)
- **lib.rs** — (no doc comment)
- **main.rs** — / Builds a per-IP token-bucket rate limit: `burst` requests allowed immediately,
- **mcp.rs** — Minimal MCP (Model Context Protocol) server: hand-rolled JSON-RPC 2.0 over
- **retention.rs** — / Hardcoded fallback when no policy row exists at all (global or per-user).
- **search.rs** — Files: tantivy full-text (name+content) ranked search, plus a LIKE fallback
- **sharing.rs** — ---------- shared row types ----------
- **state.rs** — (no doc comment)
- **storage.rs** — / Directory the master encryption key is persisted under, independent of storage backend
- **tags.rs** — / Own schema for tags/favorites, run separately from db.rs's SCHEMA array
- **tiering.rs** — Automatic hot/cold chunk tiering ("Tiering hot/cold automatique").
- **trash.rs** — ponytail: admin scoped to their own token id here too — no "view everyone's
- **tus.rs** — tus 1.0.0 resumable upload protocol (Creation + Core + Termination extensions),

## HTTP endpoints

- `/admin/audit-log`
- `/admin/groups`
- `/admin/groups/{id}`
- `/admin/groups/{id}/members/{token_id}`
- `/admin/rotate-key`
- `/admin/tokens`
- `/admin/tokens/{id}`
- `/admin/tokens/{id}/renew`
- `/audit-log/mine`
- `/comments`
- `/comments/{id}`
- `/favorites/{id}`
- `/files/upload`
- `/files/{id}`
- `/files/{id}/download`
- `/files/{id}/preview`
- `/files/{id}/restore`
- `/files/{id}/signature`
- `/files/{id}/upload-delta`
- `/files/{id}/versions`
- `/folders`
- `/folders/{id}`
- `/graphql`
- `/mcp`
- `/permissions/{id}`
- `/public/shares/{share_token}`
- `/resource-tags/{id}`
- `/search`
- `/shares`
- `/shares/{id}`
- `/tags`
- `/tags/{id}`
- `/trash`
- `/trash/{id}`
- `/tus/uploads`

## Frontends

- **client/** — client
- **admin-dashboard/** — admin-dashboard
- **file-explorer/** — file-explorer
